@model IEnumerable<FacilComercio.Models.Venda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Cli_Nome)
            </td>
           @*  <td>
                    @Html.DisplayFor(modelItem => item.Loj_Nome)
            </td> *@
            <td>
                    @Html.DisplayFor(modelItem => item.Usuario.Usu_Email)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Ven_Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Ven_Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Ven_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
